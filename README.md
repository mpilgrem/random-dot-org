# random-dot-org

Haskell bindings to the [random.org](https://random.org) Basic API (Release 4).
The API provides access to a true random number generator (TRNG) based on
atmospheric noise.

An example of use of the library is provided in the `example` folder.

This repository has no connection with Randomness and Integrity Services Ltd or
its affilates.
